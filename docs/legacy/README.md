# Legacy Reference Material

This directory marks the legacy quarantine for historical implementation notes.

The active product path is the WinUI app at `winui\FluentAgentBar.csproj`.
Use `.\scripts\verify-winui.ps1` for validation.

Historical documents may still describe old C++ behavior, old project names, or
the original porting process. They are references only and must not be treated
as the source of truth for new WinUI work.
