name: WinUI

on:
  push:
  pull_request:

jobs:
  verify-winui:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      - name: Verify WinUI
        run: .\scripts\verify-winui.ps1
